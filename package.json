{
  "name": "revenant-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Revenant Agent Box — self-hosted worker that claims jobs from Revenant Cloud and runs revenant verify",
  "scripts": {
    "start": "tsx src/main.ts",
    "dev": "tsx watch src/main.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
